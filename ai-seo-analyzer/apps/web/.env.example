# Copy to .env.local for local development.
# NEXT_PUBLIC_ variables are bundled into the browser JS - never put a
# secret here. This app has no secrets of its own; it only calls the API.
NEXT_PUBLIC_API_URL=http://localhost:8000
