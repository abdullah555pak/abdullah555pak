# shared-types

Reserved for TypeScript types generated from the API's OpenAPI schema, so
the frontend and backend can't drift out of sync on request/response
shapes. Empty until there's a real API surface worth generating types
from beyond the single foundation endpoint.
